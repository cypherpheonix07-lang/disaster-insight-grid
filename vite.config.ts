import { defineConfig } from "@lovable.dev/vite-tanstack-config";

// AegisVision AI – Vite + TanStack Start configuration.
// Uses the Lovable preset which bundles the TanStack Start Vite plugin,
// router-plugin, vite-tsconfig-paths, and SSR/prerender wiring so that
// no index.html is required.
export default defineConfig();
